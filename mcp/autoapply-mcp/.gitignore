node_modules/
dist/
coverage/
*.tsbuildinfo

# Personal campaign data: profile, resumes, database, artifacts.
# Never commit these - they contain PII and application history.
# Anchored to the repository root: unanchored, "config/" also matched
# src/config/, which silently kept seven imported source files out of the
# repository and left a fresh clone unable to build.
/config/
/data/
/artifacts/
*.sqlite
*.sqlite-journal
*.db

.env
.env.*
!.env.example

.DS_Store
Thumbs.db
